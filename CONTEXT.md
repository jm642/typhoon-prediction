# 台风路径可视化

一个作品集/学习展示目的的 Web 应用：实时展示当前活跃台风的移动路径、强度与预报。数据源为中央气象台（CMA）台风网，地图渲染用高德地图。**本 context 只关注领域概念，不含任何技术实现细节。**

## Language

**台风 (Typhoon)**：
西北太平洋上形成的热带气旋，本产品追踪与可视化的对象。
_Avoid_：热带气旋、Hurricane

**活跃台风 (Active typhoon)**：
仍被气象机构编发、有持续观测数据的台风。产品只展示活跃台风。
_Avoid_：在编台风、当前台风

**停编台风 (Stopped typhoon)**：
已停止编发的台风。不进入产品展示。

**台风编号 (Typhoon code)**：
气象机构对台风的编号，如「2614」（年份 + 当年序号）。用于对外标识。
_Avoid_：台风名（名称与人名不同，如「海葵」是名称）

**台风 ID (Typhoon ID)**：
CMA 系统的内部数值标识（如 3289093），用于获取单台风详情。与「台风编号」是两个不同概念——ID 是检索键，编号是展示标识。
_Avoid_：台风号、Id

**路径点 (Track point)**：
某个观测时次的台风中心位置与强度记录（时间、经纬度、中心气压、最大风速、风圈）。约每 3 小时一个。
_Avoid_：观测点、记录点

**实测路径 (Observed track)**：
由历史路径点连成的台风实际移动轨迹。实线渲染。

**预报路径 (Forecast track)**：
预报机构对台风未来位置的预测序列。虚线渲染，与实测路径区分。

**预报点 (Forecast point)**：
预报路径上的单个预测位置，带时距（12/24/36…120 小时）与到达时间。

**强度等级 (Intensity level)**：
CMA 对台风强度的六级分类：热带低压 → 超强台风。渲染用六级色标（绿→紫）。
_Avoid_：风力等级（这是风速的分档，不同于台风强度分级）

**中心气压 (Central pressure)**：
台风中心的海平面气压（hPa）。数值越低，台风越强。

**最大风速 (Maximum wind speed)**：
台风中心附近的最大风速（m/s）。用于判定强度等级。

**风圈 (Wind circle)**：
中心周围达到特定风速阈值（30 节 ≈ 七级风）的半径区域，按四象限（东北/东南/西南/西北）分别给出半径（km）。
_Avoid_：七级风圈半径（只是 30 节风圈的别名）

**当前中心 (Current center)**：
最新路径点的位置，地图上高亮标记的台风此刻位置。
_Avoid_：实时位置、最新位置
